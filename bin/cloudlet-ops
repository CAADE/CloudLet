#!/usr/bin/env node

var program =  require('commander');

program
  .version("0.0.1")
  .command('addCloudlet <argment>', 'add Cloudlet')
  .command('rmCloudlet <argment>', 'remove cloudlet')
  .command('lsCloudlets', 'list the cloudlets registered')
  .parse(process.argv);
