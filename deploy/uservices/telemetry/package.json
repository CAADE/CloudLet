{
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "build": "docker build . -t ${CAADE_REGISTRY}/cloudlet/telemetry",
    "deploy": "docker push ${CAADE_REGISTRY}/cloudlet/telemetry",
    "publish": "docker push ${CAADE_REGISTRY}/cloudlet/telemetry"
  },
  "dependencies": {
    "micro": "latest",
    "node-fetch": "latest"
  }
}
