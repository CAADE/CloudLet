var Promise = require('bluebird');

describe('Use Case Manage-Infrastructure', () => {
  describe('Release-Network Scenario', () => {
    it('Release-Network Primary Test', (done) => {
       done();
    });
  });
});
