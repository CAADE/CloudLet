var Promise = require('bluebird');

describe('Use Case Manage-Infrastructure', () => {
  describe('Request-Network Scenario', () => {
    it('Request-Network Primary Test', (done) => {
       done();
    });
  });
});
