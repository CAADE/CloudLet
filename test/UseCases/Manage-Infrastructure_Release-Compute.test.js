var Promise = require('bluebird');

describe('Use Case Manage-Infrastructure', () => {
  describe('Release-Compute Scenario', () => {
    it('Release-Compute Primary Test', (done) => {
       done();
    });
  });
});
