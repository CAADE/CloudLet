var Promise = require('bluebird');

describe('Use Case Manage-Infrastructure', () => {
  describe('Request-Compute Scenario', () => {
    it('Request-Compute Primary Test', (done) => {
       done();
    });
  });
});
